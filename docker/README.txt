TODO: NEEDS REWORKING




If no image is built: cd here and execute:
	docker build --rm -t shapex:latest .

If there is a shapex image and you want to run the tool:
	bash run_demo.sh

(Some explanatory notes are in docker_anaconda_stuff, the run_demo shell script explains the whole process as well)
