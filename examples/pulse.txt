# for timestep = 1.0 => duration = (l-1)*1.0

param a in [8.5,9.5];

param b in [-0.7,-0.6];
param c in [16.5,17.5];

param d in [6.5,7.5];

param e in [0.6,0.7];
param f in [-5.5,-4.5];

dparam l in [10,13];   # i.e. duration \in [(10-1)*1.0, (13-1)*1.0] = [9.0, 12.0] sec

const(a,l).line(b,c,l).const(d,l).line(e,f,l).const(a,l);