# this is nonsense, just to try out some things


sine(a,a,a,a,la).(line(a,zero,la) join line(c,zero,la))* join exp(a,b,c,la)

:

param a in (0.5,2);
param b in (-0.5,-0.3);
param c in (-1,-1);
param zero in (0,0);
duration la in (1,10);
duration lb in (1,3);

a*la -b < 5;
