sine(a,a,a,a,la).(line(a,zero,la) join line(c,zero,la))

:

param a in (0.5,2);
param b in (-0.5,-0.3);
param c in (-1,-1);
param zero in (0,0);
duration la in (1,10);
duration lb in (1,3);

abc + 3*b -ac > c + 10;
# blabla