# description

# regular expression
sinc(a,b,c,d,l)

:

# parameter declarations with ranges
param a in (0,1);
param b in (0,1);
param c in (0,0);
param d in (0,0);
duration l in (1,100);


# relational constraints
a-b <0;
