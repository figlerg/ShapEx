# description

# regular expression
sine(a,b,c,d,l)

:

# parameter declarations with ranges
param a in (0,1);
param b in (0,1);
param c in (0,1);
param d in (0,1);
duration l in (1,10);


# relational constraints
a-b <0;
c-d <0;
