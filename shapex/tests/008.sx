# description

# regular expression
(line(a,b,l2).const(b,l).sine(a,b,c,d,l)) union (sinc(a,b,c,d,l).exp(a,b,c,l2))

:

# parameter declarations with ranges
param a in (0,1);
param b in (0,1);
param c in (0,1);
param d in (0,1);
duration l in (15,15);
duration l2 in (5,5);


# relational constraints
a-b <0;
